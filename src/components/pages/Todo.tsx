import { FC } from "react";

import Layout from "../templetes/Layout";

const Todo: FC = () => {
  return (
    <Layout>
      <div className="flex-1">Todo</div>
    </Layout>
  );
};

export default Todo;
